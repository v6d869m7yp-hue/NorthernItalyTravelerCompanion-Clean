# v7.1.0 — Unified Journey Navigation

- Adds a persistent **Istanbul** return button throughout Northern Italy.
- Adds a direct **Cruise: Venice** button for returning to the cruise handoff point.
- Uses large, touch-friendly controls on iPad and phones.
- Keeps the existing v7.0.1 itinerary, content, maps, reservations, and design intact.

The cruise site URL is centralized in `assets/app.js` as `cruiseBase` for easy correction if the GitHub Pages repository name differs.
