# Cloud Functions milestone

No production Cloud Function is deployed in v6.0. The next backend milestone will add callable HTTPS functions for traveler invitations, WebAuthn/passkey registration challenges, authentication challenges, device revocation, vault-key rotation, and temporary QR sessions.

Server credentials must be supplied by the Firebase runtime and must never be copied into the GitHub Pages application.
