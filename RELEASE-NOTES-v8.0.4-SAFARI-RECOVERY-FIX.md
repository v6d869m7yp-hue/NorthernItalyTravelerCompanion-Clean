# v8.0.4 — Safari Recovery Fix

- Prevents benign Safari and browser-extension events from showing the red fatal-error banner.
- Records genuine same-origin Companion errors for review in Diagnostics.
- Adds a Recent runtime issues section with a clear-log control.
- Preserves the v8.0.3 multi-file reservation upload feature.
- Bumps the embedded Companion cache to v10.0.1 so GitHub Pages devices receive the fix.
