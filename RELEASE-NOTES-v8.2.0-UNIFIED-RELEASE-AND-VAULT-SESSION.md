# Northern Italy Traveler’s Companion v8.2.0

Unified Release & Vault Session

- One public version number across Northern Italy, Istanbul, cruise, Venice, Companion, Diagnostics, cache metadata, and footers.
- Removes the separate v10.x Companion numbering from the active interface.
- Uses a 30-minute default Vault inactivity window.
- Resets the inactivity timer during normal use instead of only on the first interaction.
- No longer locks the Vault merely because Safari opens a file picker or briefly backgrounds the page.
- The Vault still locks manually and after the configured inactivity period.
